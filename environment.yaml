name: rnaseq-downstream
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - pandas
  - numpy
  - matplotlib
  - seaborn
  - scikit-learn
  - pydeseq2
  - gseapy
  - pyyaml
  - pip
  - pip:
    # Add any pip-specific packages here if needed
